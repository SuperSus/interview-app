<br>
<div class="col-lg-10">
  <br>
  <div id="pagtop">
    <div id="paginator">
      <%= paginate @movies %>
    </div>
  </div>
  <div id="paginate">
    <%= render 'paginate_movies' %>
  </div>
  <div id="pagfooter">
    <div id="paginator">
      <%= paginate @movies %>
    </div>
  </div>
</div>
<br>
<div class="col-md-2">
  <div class="d-flex justify-content-center">
    <% if logged_in?(:admin) %>
      <p><%= button_to 'Add new movie', new_movie_path, method: :get, class: "btn btn-outline-success" %></p>
    <% end %>
  </div>
  <br>
  <%= render 'filter_form' %>
  <br>
  <div class="d-flex justify-content-center">
    <p><%= flash_message %></p>
  </div>
</div>

<br>
<div class="col-lg-10">
  <%= flash_message %>
  <div id="pagtop">
    <div id="paginator">
      <%= paginate @movies %>
    </div>
  </div>
  <div id="paginate">
    <%= render 'paginate_movies' %>
  </div>
  <div id="pagfooter">
    <div id="paginator">
      <%= paginate @movies %>
    </div>
  </div>
</div>
<br>
<div class="col-md-2">
  <div class="d-flex justify-content-center">
    <%= button_to 'Add new movie', new_movie_path, method: :get, class: "btn btn-outline-success" %>
  </div>
</div>
